<h1>Breweries</h1>
<p> Number of breweries: <%= @breweries.count %> </p>
<p> Number of active breweries: <%= @active_breweries.count %> </p>

<%= render 'list', breweries: @active_breweries %>

<p> Number of inactive breweries: <%= @retired_breweries.count %> </p>

<%= render 'list', breweries: @retired_breweries %>

<br>

<%= link_to 'New Brewery', new_brewery_path, class:'btn btn-light'  unless current_user.nil?%>
<%= link_to 'list of beers', beers_path,  class:'btn btn-light'%>
