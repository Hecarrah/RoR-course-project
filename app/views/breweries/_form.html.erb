<%= form_with(model: brewery, local: true) do |form| %>
  <% if brewery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brewery.errors.count, "error") %> prohibited this brewery from being saved:</h2>

      <ul>
      <% brewery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :brewery_name, class:"form-control" %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :brewery_year, class:"form-control" %>
  </div>

  <div class="field">
    <%= form.check_box :active, class:"" %>
    <%= form.label :active,class:"" %>
  </div>

  <button type="submit" class="btn btn-primary">Create Brewery</button>


<% end %>
